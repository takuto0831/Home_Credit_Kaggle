---
title: "Home Credit Dafault Risk (EDA)"
author: "kotsubo takuto"
output: 
    html_document:
      md_extensions: -ascii_identifiers
      toc: true
      toc_depth: 3
---

# Home Credit Default Risk

## Overview

- 多くの人々は, クレジット履歴の不足や欠如により, ローン契約に奮闘している.
そして不幸なことにこのような人々はしばしば, 信頼できない貸し手を利用している.

- そこでクレジット履歴に代替する, 電気通信会社や取引情報などを用いて, 対象者の返済能力を予測する.

## Data description

- application_{train|test}.csv (122|121 variables)
    - Key variable: SK_ID_CURR
    - Objective variable: TARGET(1:返済困難, 0: other, 連続値)
    - About: 全てのアプリケーションのデータ
- bureau.csv (17 variables)
    - Key variable: SK_ID_CURR, SK_ID_BUREAU 
    - About: 以前使用していた, 融資の履歴
- bureau_balance.csv (3 variables)
    - Key variable: SK_ID_BUREAU
    - About: `bureau.csv`の補助データ
    - MONTHS_BALANCE: 利用日に対応した, 月次のバランス?
    - STATUS: 月次のステータス, (active, closed, DPD0-30,… [C means closed, X means status unknown, 0 means no DPD, 1 means maximal did during month between 1-30, 2 means DPD 31-60,… 5 means DPD 120+ or sold or written off ] )
- previous_applications.csv (37 variables)
    - Key variable: SK_ID_CURR, SK_ID_PREV
    - About: サンプルで融資を受けているお客様のホームクレジットローンのすべての以前のアプリケーション
- installments_payment.csv (8 variables)
    - Key variable: SK_ID_CURR, SK_ID_PREV
    - About: 
- POS_CASH_balance.csv (8 variables)
    - Key variable: SK_ID_CURR, SK_ID_PREV
    - About: 
- credit_card_balance.csv (23 variavbles)
    - Key variable: SK_ID_CURR, SK_ID_PREV
    - About: 

# Toc

- About Competition
    - overview
    - data description
- Set up
    - library package
    - load function
    - read csv
- Preprocessing
    - charcter type -> factor type  
    - Missing values count
    - 重要なカラムの選択
    - 特徴量抽出
- EDA
    - データ内の課題発見
    - データの特性発見
- Kernel sample
    - [xgboost](http://rtokei.tech/machine-learning/機械学習アルゴリズム〜xgboost〜/) : xgboostではキャルクター扱えない??
- Data analysis

# Setting

## knitr option

```{r reset, include=FALSE}
# 初期化
rm(list = ls())
```

```{r set up, message=FALSE}
# set directory
setwd("~/Desktop/Home_Credit_Kaggle/") 
# max.print 
options(max.print="200", digits=5)
# Global options
library(knitr)
opts_chunk$set(echo=TRUE,
               cache = TRUE,
	             prompt=FALSE,
               tidy=TRUE,
               comment=NA,
               message=FALSE,
               warning=FALSE)
opts_knit$set(width=75)
```

## Library package

- 必要なパッケージを適宜追加

```{r package, message=FALSE}
# install_github("hadley/readr")
library(tidyverse)
library(readr) # for csv
library(summarytools) # summary easily for EDA
library(GGally) # ggpairs
library(skimr)
```

## Load funciton

```{r}
source('~/Desktop/Home_Credit_Kaggle/function/Preprocessing.R') # for preprocessing
```

## read csv

- Missing value
na = c("XNA","NA","","NaN","?"): 以下の設定で naを計上する. (application_train のfactor列は確認済み)

```{r}
# 訓練データ, テストデータ
application_train <- read_csv("csv/application_train.csv",na = c("XNA","NA","","NaN","?")) %>% 
    mutate_if(is.character, funs(factor(.)))
application_test <- read_csv("csv/application_test.csv",na = c("XNA","NA","","NaN","?")) %>% 
  mutate_if(is.character, funs(factor(.)))

# クレジット利用履歴 (by Credit Bureau)
bureau <- read_csv("csv/bureau.csv") %>% 
  mutate_if(is.character, funs(factor(.)))
bureau_balance <- read_csv("csv/bureau_balance.csv") %>% 
  mutate_if(is.character, funs(factor(.)))

# 過去の利用データ
previous_application <- read_csv("csv/previous_application.csv") %>% 
  mutate_if(is.character, funs(factor(.)))
installments_payments <- read_csv("csv/installments_payments.csv") %>% 
  mutate_if(is.character, funs(factor(.)))
POS_CASH_balance <- read_csv("csv/POS_CASH_balance.csv") %>% 
  mutate_if(is.character, funs(factor(.)))
credit_card_balance <- read_csv("csv/credit_card_balance.csv") %>% 
  mutate_if(is.character, funs(factor(.)))

# submit style file
# sample_submission <- read_csv("csv/sample_submission.csv")
```

# Preprocessing

## Check Missing value

```{r}
CheckMissingValue(application_train)
```

# EDA

## Check Summary and missing value

```{r}
library(skimr)
application_train %>% skim() %>% kable()
application_test %>% skim() %>% kable()
bureau %>% skim() %>% kable()
bureau_balance %>% skim() %>% kable()
previous_application %>% skim() %>% kable()
installments_payments %>% skim() %>% kable()
POS_CASH_balance %>% skim() %>% kable()
credit_card_balance %>% skim() %>% kable()
```

```{r}
dfSummary(application_train) %>% 
  view(method = "render")
dfSummary(bureau) %>% 
  view(method = "render")
dfSummary(bureau_balance) %>% 
  view(method = "render")
dfSummary(previous_application) %>% 
  view(method = "render")
dfSummary(installments_payments) %>% 
  view(method = "render")
dfSummary(POS_CASH_balance) %>% 
  view(method = "render")
dfSummary(credit_card_balance) %>% 
  view(method = "render")
```

## Relational plot

```{r}
application_train %>% 
  select(FLAG_OWN_CAR,FLAG_OWN_REALTY,FLAG_CONT_MOBILE) %>% 
  ggpairs(upper = list(continuous = wrap("cor", size = 5)),                    
          lower = list(continuous = "smooth"))

application_train %>% 
  select(AMT_INCOME_TOTAL,AMT_CREDIT,AMT_ANNUITY,AMT_GOODS_PRICE) %>% 
  ggpairs(upper = list(continuous = wrap("cor", size = 10)),                    
          lower = list(continuous = "smooth"))

```


```{r}
application_train[2:6,] %>%
  select(-SK_ID_CURR) %>% 
  ggpairs()
```





