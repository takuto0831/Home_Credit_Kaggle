---
title: "Home Credit Dafault Risk (EDA)"
author: "kotsubo takuto"
output: 
    html_document:
      md_extensions: -ascii_identifiers
      toc: true
      toc_depth: 3
---

# Home Credit Default Risk

## Overview

- 多くの人々は, クレジット履歴の不足や欠如により, ローン契約に奮闘している.
そして不幸なことにこのような人々はしばしば, 信頼できない貸し手を利用している.

- そこでクレジット履歴に代替する, 電気通信会社や取引情報などを用いて, 対象者の返済能力を予測する.

## Data description

- application_{train|test}.csv (122|121 variables)
    - Key variable: SK_ID_CURR
    - Objective variable: TARGET(1:返済困難, 0: other)
    - About: 全てのアプリケーションのデータ
- bureau.csv (17 variables)
    - Key variable: SK_ID_CURR, SK_ID_BUREAU 
    - About: 以前使用していた, 融資の履歴
- bureau_balance.csv (3 variables)
    - Key variable: SK_ID_BUREAU
    - About: `bureau.csv`の補助データ
    - MONTHS_BALANCE: 利用日に対応した, 月次のバランス?
    - STATUS: 月次のステータス, (active, closed, DPD0-30,… [C means closed, X means status unknown, 0 means no DPD, 1 means maximal did during month between 1-30, 2 means DPD 31-60,… 5 means DPD 120+ or sold or written off ] )
- previous_applications.csv (37 variables)
    - Key variable: SK_ID_CURR, SK_ID_PREV
    - About: サンプルで融資を受けているお客様のホームクレジットローンのすべての以前のアプリケーション
- installments_payment.csv (8 variables)
    - Key variable: SK_ID_CURR, SK_ID_PREV
    - About: 
- POS_CASH_balance.csv (8 variables)
    - Key variable: SK_ID_CURR, SK_ID_PREV
    - About: 
- credit_card_balance.csv (23 variavbles)
    - Key variable: SK_ID_CURR, SK_ID_PREV
    - About: 

# To do list

- EDA
    - データ内の課題発見
    - データの特性発見
- データ前処理
- Kernelを参考に, xgboostなどを実行してみる
- データ分析(後述)

# Setting

## knitr option

```{r reset}
rm(list = ls())
```

```{r set up}
# set directory
setwd("~/Desktop/Home_Credit_Kaggle/") 
# max.print 
options(max.print="100", digits=5)
# Global options
library(knitr)
opts_chunk$set(echo=TRUE,
               cache = TRUE,
	             prompt=FALSE,
               tidy=TRUE,
               comment=NA,
               message=FALSE,
               warning=FALSE)
opts_knit$set(width=75)
```

## Library package

```{r message=FALSE}
library(tidyverse)
library(readr) # for csv
library(summarytools) # summary easily for EDA
```

## Load funciton


## read csv

```{r}
# 訓練データ, テストデータ
application_test <- read_csv("csv/application_test.csv")
application_train <- read_csv("csv/application_train.csv")

# クレジット利用履歴 (by Credit Bureau)
bureau <- read_csv("csv/bureau.csv")
bureau_balance <- read_csv("csv/bureau_balance.csv")

# 過去の利用データ
previous_application <- read_csv("csv/previous_application.csv")
installments_payments <- read_csv("csv/installments_payments.csv")
POS_CASH_balance <- read_csv("csv/POS_CASH_balance.csv")
credit_card_balance <- read_csv("csv/credit_card_balance.csv")

# submit style file
# sample_submission <- read_csv("csv/sample_submission.csv")
```

# EDA

## Check Summary and missing value

```{r}
dfSummary(application_train) %>% 
  view(method = "render")

dfSummary(bureau) %>% 
  view(method = "render")
dfSummary(bureau_balance) %>% 
  view(method = "render")

dfSummary(previous_application) %>% 
  view(method = "render")
dfSummary(installments_payments) %>% 
  view(method = "render")
dfSummary(POS_CASH_balance) %>% 
  view(method = "render")
dfSummary(credit_card_balance) %>% 
  view(method = "render")
```




